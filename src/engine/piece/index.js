const { Bishop } = require('./Bishop')
const { King } = require('./King')
const { Knight } = require('./Knight')
const { Pawn } = require('./Pawn')
const { Queen } = require('./Queen')
const { Rook } = require('./Rook')

const { Color } = require('./Piece')

module.exports = {
    Bishop,
    King,
    Knight,
    Pawn,
    Queen,
    Rook,
    Color,
}